  "data": [{
      "id": 6,
      "incident_id": 6,
      "status": 4,
      "message": "Résolu",
  }]

  Incident Updates​
  GET / api / v1 / incidents / { incidentId }
  /updates
  GET / api / v1 / incidents / { incidentId }
  /updates/ { incidentUpdateId }
  POST / api / v1 / incidents / { incidentId }
  /updates
  PUT / api / v1 / incidents / { incidentId }
  /updates/ { incidentUpdateId }
  DELETE / api / v1 / incidents / { incidentId }
  /updates/ { incidentUpdateId }